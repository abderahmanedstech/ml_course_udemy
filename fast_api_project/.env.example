# Path to your saved model (joblib/pkl)
MODEL_PATH=models/model.joblib

# Optional comma-separated list of feature names used during training.
# If set, the API will auto-fill missing features with 0 and enforce column order.
# FEATURE_NAMES=Age,Fare,Pclass,Sex_male,...
